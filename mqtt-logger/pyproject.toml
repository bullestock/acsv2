[project]
name = "mqtt-logger"
version = "0.1.0"
description = "Subscribe to MQTT log messages and write them to a file"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "paho-mqtt>=2.1.0",
]
