[project]
name = "mqtt-bridge"
version = "0.1.0"
description = "MQTT bridge"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "certifi>=2026.5.20",
    "paho-mqtt>=2.1.0",
    "requests>=2.34.2",
]

