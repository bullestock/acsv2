CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

SET(PROJECT ccdemo)

PROJECT(${PROJECT})

INCLUDE_DIRECTORIES(. ../../include/)

SET(SRCS
    cardcache.cpp
    main.cpp
)

ADD_EXECUTABLE(ccdemo ${SRCS})

TARGET_LINK_LIBRARIES(${PROJECT}
    fmt
    restclient-cpp
)

#SET(TARGET_HOST 192.168.1.229)

#ADD_CUSTOM_COMMAND(TARGET gpiodemo POST_BUILD COMMAND scp $<TARGET_FILE:gpiodemo> ${TARGET_HOST}:)
